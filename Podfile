# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
platform :ios, '15.0' # or your project's minimum iOS versi

target 'EATAR' do
  # Comment the next line if you don't want to use dynamic frameworks
  # use_frameworks!

  # Pods for EATAR
    pod 'Alamofire', '~> 5.6' # Latest compatible version

  target 'EATARTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'EATARUITests' do
    # Pods for testing
  end

end
